import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vechilebooking.kinglong',
  appName: 'King Long Booking',
  webDir: 'dist'
};

export default config;
